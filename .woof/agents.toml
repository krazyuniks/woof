# Woof runtime settings. Dispatch routing lives in .woof/policy.toml.

[timeouts]
default_minutes = 30

[review_valve]
every_n_work_units = 5
end_of_epic = true

[audit]
enabled = true
max_bytes = 262144
redact_patterns = []
