# Go cartography fragment (composed by `woof init` into scripts/refresh-cartography).
# Registers the ctags language for .go sources so the mechanical tags index
# covers Go symbols.
woof_add_ctags_language Go
