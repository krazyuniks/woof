# Rust cartography fragment (composed by `woof init` into scripts/refresh-cartography).
# Registers the ctags language for .rs sources so the mechanical tags index
# covers Rust symbols.
woof_add_ctags_language Rust
