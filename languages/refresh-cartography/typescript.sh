# TypeScript cartography fragment (composed by `woof init` into scripts/refresh-cartography).
# Registers the ctags language for .ts/.tsx sources so the mechanical tags index
# covers TypeScript symbols.
woof_add_ctags_language TypeScript
