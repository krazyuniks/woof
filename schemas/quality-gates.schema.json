{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://woof.dev/schemas/quality-gates.schema.json",
  "title": "woof quality-gates configuration",
  "description": "Project-side declaration at .woof/quality-gates.toml. Tells Stage 5 Check 1 (project quality command) what to run after each story to verify build/lint/type/test all pass. Single command per gate. Blocking gates must exit 0 within their declared timeout or Stage 5 Check 1 fails; advisory gates (blocking = false) record a minor finding on non-zero exit and continue.",
  "type": "object",
  "additionalProperties": false,
  "required": ["gates"],
  "properties": {
    "default_mode": {
      "type": "string",
      "enum": ["strict", "baseline"],
      "description": "Project-level default mode applied to every gate that does not declare its own mode. Omitting this field is equivalent to 'strict'. Baseline is opt-in for brownfield repos with known-red gates."
    },
    "gates": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "$ref": "#/$defs/gate" },
      "description": "Named quality gates run in declaration order. Suggested keys: build, lint, type, test."
    }
  },
  "$defs": {
    "gate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command"],
      "properties": {
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Shell command. Run from repo root. Blocking gates must exit 0 to pass; advisory gates (blocking = false) record a minor finding on non-zero exit and do not fail Stage 5 Check 1."
        },
        "timeout_seconds": {
          "type": "integer",
          "minimum": 1,
          "description": "Hard timeout. Hitting it is treated as a check failure."
        },
        "blocking": {
          "type": "boolean",
          "default": true,
          "description": "If false, a non-zero exit logs a finding but does not fail Stage 5 Check 1."
        },
        "mode": {
          "type": "string",
          "enum": ["strict", "baseline"],
          "description": "Per-command mode. 'strict': any failure blocks (default). 'baseline': a command already red at capture time is reported but does not block, provided its command identity is unchanged and a durable baseline record exists. Overrides default_mode for this gate."
        }
      }
    }
  }
}
